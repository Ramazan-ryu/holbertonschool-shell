#!/usr/bin/env bash 
#
smt=0
while [ $smt -lt 10 ]
do
	if [ $smt == 3 ]
	then
		echo "bad luck"
	elif [ $smt == 7 ]
	then
		echo "good luck"
	else
	      	echo "Best School"
	fi
	       	((smt++ ))
done
